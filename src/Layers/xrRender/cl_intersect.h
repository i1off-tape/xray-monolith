//---------------------------------------------------------------------------
// Consolidated: the geometry intersection helpers live in a single canonical
// header to avoid maintaining byte-for-byte copies across projects.
#include "../../xrEngine/cl_intersect.h"
